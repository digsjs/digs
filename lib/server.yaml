server:
  app:
    namespace: digs
    project: home
    coap:
      port: 5683
      host: localhost
  connections:
    router:
      stripTrailingSlash: true
    app:
      port: 9090
      host: localhost
plugins:
  -
    ./lib/plugin: {}

connections:
  -
    port: 9090
    labels:
      - http
