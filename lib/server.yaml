server:
  app:
    namespace: digs
    project: home
    coap:
      port: 5683
      host: localhost
  connections:
    router:
      stripTrailingSlash: true

plugins:
  -
    ./lib/plugin: {}

connections:
  -
    port: 9090
    host: localhost
    labels:
      - http
